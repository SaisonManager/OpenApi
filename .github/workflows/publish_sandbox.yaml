name: Sandbox Publish

on:
  push:
    branches:
      - main
    tags:
      - '*'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  sandbox-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write 
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node.js 
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # ==========================================
      # 1. CALCULATE DYNAMIC VARIABLES
      # ==========================================
      - name: Determine Variables (Version & Names)
        id: vars
        run: |
          # --- A. Version Calculation ---
          EXACT_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "")
          if [ -n "$EXACT_TAG" ]; then
            VERSION="$EXACT_TAG"
          else
            RECENT_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
            if [ -z "$RECENT_TAG" ]; then
              RECENT_TAG="0.0.0"
              DEV_NUM=$(git rev-list HEAD --count)
            else
              DEV_NUM=$(git rev-list ${RECENT_TAG}..HEAD --count)
            fi
            SHORT_HASH=$(git rev-parse --short HEAD)
            VERSION="${RECENT_TAG}-dev-${DEV_NUM}-${SHORT_HASH}"
          fi
          
          # Strip leading 'v'
          VERSION=${VERSION#v}
          echo "PACKAGE_VERSION=$VERSION" >> $GITHUB_ENV
          
          # --- B. Scopes and Names (Strict Lowercase for NPM) ---
          OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          
          # Export globally so subsequent steps can use them safely
          echo "NPM_SCOPE=@${OWNER_LC}" >> $GITHUB_ENV
          echo "NPM_PACKAGE_NAME=@${OWNER_LC}/saisonmanager-api-v2" >> $GITHUB_ENV
          echo "NUGET_PACKAGE_NAME=SaisonManager.Api.V2" >> $GITHUB_ENV

      # ==========================================
      # 2. GENERATE CODE
      # ==========================================
      - name: Generate Clients
        run: |
          # C#
          npx @openapitools/openapi-generator-cli generate \
            -i openapi.json -g csharp -o ./generated/csharp \
            --additional-properties=packageName=${{ env.NUGET_PACKAGE_NAME }},packageVersion=${{ env.PACKAGE_VERSION }},targetFramework=net10.0,library=httpclient,useSystemTextJson=true,nullableReferenceTypes=true

          # TypeScript
          npx @openapitools/openapi-generator-cli generate \
            -i openapi.json -g typescript-fetch -o ./generated/typescript \
            --additional-properties=npmName=${{ env.NPM_PACKAGE_NAME }},npmVersion=${{ env.PACKAGE_VERSION }},supportsES6=true

      # ==========================================
      # 3. STITCH AND FIX README
      # ==========================================
      - name: Process and Attach READMEs
        run: |
          # Find the generated API doc (fails safely if missing)
          API_MD_CS=$(find ./generated/csharp -name "*Api.md" | head -n 1)
          API_MD_TS=$(find ./generated/typescript -name "*Api.md" | head -n 1)
          
          if [ -z "$API_MD_CS" ]; then echo "Error: C# API documentation not found."; exit 1; fi

          # 1. Build C# README
          echo "# ${{ env.NUGET_PACKAGE_NAME }}" > csharp_readme.md
          echo -e "\nOfficial Client for the SaisonManager API.\n" >> csharp_readme.md
          cat "$API_MD_CS" >> csharp_readme.md

          # 2. Build TypeScript README (Use its own API MD if available, else fallback to C#)
          echo "# ${{ env.NPM_PACKAGE_NAME }}" > ts_readme.md
          echo -e "\nOfficial Client for the SaisonManager API.\n" >> ts_readme.md
          if [ -n "$API_MD_TS" ]; then
            cat "$API_MD_TS" >> ts_readme.md
          else
            cat "$API_MD_CS" >> ts_readme.md
          fi

          # 3. Link Correction Function
          fix_links() {
            local file=$1
            # Remove self-references
            sed -i 's/[a-zA-Z0-9]*Api\.md#/#/g' "$file"
            # Convert [Model](Model.md) to [Model](#model)
            sed -i -E 's/\[([^]]+)\]\(([a-zA-Z0-9_.-]+)\.md\)/[\1](#\L\2)/g' "$file"
            # Remove broken relative "Back to..." links
            sed -i 's|\[\[Back to README\]\](\.\.\/\.\.\/README\.md)||g' "$file"
            sed -i 's|\[\[Back to API list\]\](\.\.\/\.\.\/README\.md#documentation-for-api-endpoints)||g' "$file"
            sed -i 's|\[\[Back to Model list\]\](\.\.\/\.\.\/README\.md#documentation-for-models)||g' "$file"
          }

          fix_links csharp_readme.md
          fix_links ts_readme.md

          # 4. Distribute to Project Folders
          CSPROJ_FILE=$(find ./generated/csharp -name "*.csproj" | grep -v "Test" | head -n 1)
          PROJECT_DIR=$(dirname "$CSPROJ_FILE")
          
          cp csharp_readme.md "$PROJECT_DIR/README.md"
          cp csharp_readme.md "./generated/csharp/README.md"
          cp ts_readme.md "./generated/typescript/README.md"
          
          # 5. Copy to Docs folder for GitHub Pages
          cp csharp_readme.md ./docs/csharp.md
          cp ts_readme.md ./docs/typescript.md

          # 6. Safely Inject into .csproj without breaking XML
          sed -i '/<PackageReadmeFile>/d' "$CSPROJ_FILE"
          sed -i '/<None Include="README.md"/d' "$CSPROJ_FILE"
          
          # Inject property
          sed -i '/<\/PropertyGroup>/i \    <PackageReadmeFile>README.md</PackageReadmeFile>' "$CSPROJ_FILE"
          
          # Append ItemGroup robustly
          sed -i 's/<\/Project>//g' "$CSPROJ_FILE"
          cat <<EOF >> "$CSPROJ_FILE"
            <ItemGroup>
              <None Include="README.md" Pack="true" PackagePath="/" />
            </ItemGroup>
          </Project>
          EOF

      # ==========================================
      # 4. BUILD AND PUBLISH C#
      # ==========================================
      - name: Build and Pack C#
        working-directory: ./generated/csharp
        run: dotnet pack -c Release

      - name: Publish C#
        working-directory: ./generated/csharp
        run: |
          # Added --skip-duplicate to gracefully handle workflow re-runs
          dotnet nuget push src/*/bin/Release/*.nupkg \
            --api-key ${{ secrets.GITHUB_TOKEN }} \
            --source "https://nuget.pkg.github.com/${{ github.repository_owner }}/index.json" \
            --skip-duplicate

      # ==========================================
      # 5. BUILD AND PUBLISH TYPESCRIPT
      # ==========================================
      - name: Build and Publish TypeScript
        working-directory: ./generated/typescript
        run: |
          # Use the globally exported NPM_SCOPE for a perfectly scoped registry
          echo "${{ env.NPM_SCOPE }}:registry=https://npm.pkg.github.com" > .npmrc
          echo "//npm.pkg.github.com/:_authToken=\${NODE_AUTH_TOKEN}" >> .npmrc
          
          npm install
          npm run build
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ==========================================
      # 6. COMMIT DOCS UPDATES
      # ==========================================
      - name: Commit and Push Documentation
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          # Use --allow-empty in case no changes occurred
          git commit -m "docs: update sdk documentation [skip ci]" || echo "No changes to commit"
          # Ensure we are on the main branch before pushing
          git checkout -B main
          git push origin main